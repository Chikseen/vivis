name: Node.js CI

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: self-hosted
        
    steps:
      - uses: actions/checkout@v2
      - run: docker container ls
      - run: pwd
      - run: ls
      - run: docker-compose up -d --build